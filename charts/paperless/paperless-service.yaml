apiVersion: v1
kind: Service
metadata:
  name: paperless
  namespace: paperless
  labels:
    app: paperless
spec:
  ports:
    - port: 80
      name: http
  clusterIP: None
  selector:
    app: paperless
